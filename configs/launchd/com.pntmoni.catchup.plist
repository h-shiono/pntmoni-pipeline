<?xml version="1.0" encoding="UTF-8"?>
<!--
  launchd agent template for the nightly PNT Moni operational run
  (catchup = current day's daily + N backfill gap days).

  This is a TEMPLATE: replace __REPO__ with the absolute repo path
  (e.g. /Users/hayato/dev/pntmoni-pipeline) before installing. See
  configs/launchd/README.md for install + machine-wake instructions.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.pntmoni.catchup</string>

    <key>ProgramArguments</key>
    <array>
        <string>__REPO__/scripts/run_catchup.sh</string>
    </array>

    <key>WorkingDirectory</key>
    <string>__REPO__</string>

    <!-- Run nightly at 03:00 local time. daily + 2 backfill days fits a
         typical overnight window (~4.5 h). -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>3</integer>
        <key>Minute</key>
        <integer>0</integer>
    </dict>

    <key>RunAtLoad</key>
    <false/>

    <key>StandardOutPath</key>
    <string>__REPO__/data/logs/launchd.catchup.out.log</string>
    <key>StandardErrorPath</key>
    <string>__REPO__/data/logs/launchd.catchup.err.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    </dict>
</dict>
</plist>
