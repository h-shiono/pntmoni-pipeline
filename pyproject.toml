[project]
name = "pntmoni-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "h-shiono", email = "hato.gnss@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
pntmoni-pipeline = "pntmoni_pipeline:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
